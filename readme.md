# Monitor de Estado de Servicios

Monitor simple de estado y latencia de sitios web.

- Abrir `index.html` en el navegador para ver el tablero.
- Más detalles y configuración en la carpeta `docs/`.
